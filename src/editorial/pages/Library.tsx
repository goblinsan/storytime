import PlaceholderSurface from '../components/PlaceholderSurface';

export default function Library() {
  return (
    <PlaceholderSurface
      name="library"
      title="Library"
      description="Every derivative work across all universes, grouped by format."
    />
  );
}
