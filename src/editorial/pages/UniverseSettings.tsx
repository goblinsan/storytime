import PlaceholderSurface from '../components/PlaceholderSurface';

export default function UniverseSettings() {
  return (
    <PlaceholderSurface
      title="Universe settings"
      description="Theme, protection audit, guardrails and autonomy locks."
    />
  );
}
